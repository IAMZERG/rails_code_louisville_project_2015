<%= form_for(@decklist) do |f| %>
  <% if @decklist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@decklist.errors.count, "error") %> prohibited this decklist from being saved:</h2>

      <ul>
      <% @decklist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>





<% @decklist.cards.each do |c| %>
  <%= form_for(c) do |card| %>
    <div class="field">
      <%= card.label :quantity %><br>
      <%= card.text_field :quantity %>
    </div>
    <div class="field">
      <%= card.label :name %><br>
      <%= card.text_field :name %>
    </div>
    <div class="actions">
      <%= card.submit %>
    </div>
    <%= link_to "add card", new_decklist_card_path(@decklist.id) %>
  <% end %>
<% end %>
